import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // MindMate brand colors
  static const Color navy = Color(0xFF1D2B3A);
  static const Color mint = Color(0xFF65B594);

  // Splash background
  static const Color background = Color(0xFFFCFCFC);

  // Light decorative green
  static const Color lightMint = Color(0xFFEFF7F3);

  // Very light border
  static const Color borderMint = Color(0xFFDDEBE5);

  // Text
  static const Color textDark = Color(0xFF1D2B3A);
}